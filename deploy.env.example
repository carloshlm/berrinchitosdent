# Copiá este archivo a deploy.env y completá con los datos reales.
# deploy.env está en .gitignore — nunca se commitea.

DEPLOY_HOST=203.0.113.10
DEPLOY_USER=deploy
DEPLOY_PATH=/opt/sites/berrinchitosdent.com/public/
DEPLOY_KEY=$HOME/.ssh/tu_clave_privada
